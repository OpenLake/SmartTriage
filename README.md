# SmartTriage
SmartTriage is an ML bot that keeps OpenLake maintainers focused on shipping, not clutter. It uses semantic search to catch duplicate issues even when phrased differently, and scans commit history to auto‑assign the right reviewer. By automating triage, it speeds collaboration and showcases a real‑world NLP pipeline.
